export type TranslationKeys = {
    INITIAL_SCREEN_VIEW: {
        CREATE_GAME: string;
        JOIN_GAME: string;
    },
    CREATE_GAME_VIEW: {
        NAME: string;
        CREATE_GAME: string;
    },
    JOIN_GAME_VIEW: {
        NAME: string;
        CODE: string;
        JOIN_GAME: string;
    }
}